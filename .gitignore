# Build output — produced by build.sh / swift build, not source.
/Budgie.app/
/.build/

# Build prerequisites — set up per the README, not part of this repo.
# CrispASR and ParakeetCpp are their own git repos; models are large downloads.
/CrispASR/
/ParakeetCpp/
/parakeet.cpp/
/models/

# SwiftPM local state
/.swiftpm/

# macOS cruft
.DS_Store
